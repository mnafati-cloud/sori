/* Sori — story-data.js : le feuilleton « 이야기 ». GÉNÉRÉ par tools/story_build.mjs à partir des
   chapitres ÉCRITS À LA MAIN, et seulement s'ils passent les deux plafonds (chaque lemme dans le
   vocabulaire maîtrisé, chaque structure dans l'acquis + la cible du chapitre, forme du chapitre).
   Corpus vide = l'écran Histoire ne s'affiche pas du tout. Ne pas éditer à la main. */
(function(root){
  "use strict";
  const STORY_DATA = { saison: 1, chapitres: [] };
  if (typeof module !== "undefined" && module.exports) module.exports = STORY_DATA;
  else root.STORY_DATA = STORY_DATA;
})(typeof self !== "undefined" ? self : this);
